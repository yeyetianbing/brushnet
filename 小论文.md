# Abstract

文本引导图像修复旨在根据用户提示，在指定掩膜区域内生成语义准确、视觉真实且与周围内容协调的图像。现有扩散式修复方法虽具备较强的生成先验，但在大面积缺失、不规则掩膜和复杂纹理场景中，仍容易出现提示语义偏移、局部结构不完整以及修复区域与已知背景衔接不自然等问题。为此，本文提出一种融合自适应残差注意力与推理阶段多奖励引导的双分支潜空间扩散修复框架。该框架以预训练扩散 UNet 作为冻结的生成主干，通过辅助条件分支独立建模带噪潜变量、掩膜图像特征与空间掩膜，并将提取到的像素级条件信息以逐层、多尺度方式注入去噪主干。在此基础上，本文设计自适应残差注意力融合模块（Adaptive Residual Attention Fusion，ARAF），分别从通道和空间维度对辅助分支的层级条件残差进行自适应重标定，并通过可学习的残差权重平衡注意力增强特征与原始特征，以提高多尺度条件注入的有效性与稳定性。进一步地，本文在推理阶段引入三元奖励引导策略（Tri-reward Guidance，TriRG），利用全局图文偏好、掩膜区域局部语义一致性和修复视觉合理性三类可微奖励，分别计算各奖励对当前潜变量的梯度并进行幅值均衡，以缓解异构奖励尺度差异导致的单一目标主导问题。均衡后的联合梯度用于对部分去噪步的中间潜变量进行在线校正，在不改变训练流程的条件下进一步增强提示对齐和生成内容的视觉可信度。BrushBench 上的初步实验表明，ARAF 在仅引入较小额外参数的情况下能够改善条件特征表达，并且与 TriRG 结合后可进一步提升生成结果的文本对齐度与人类偏好评分，验证了结构级条件增强与采样级奖励引导的互补性。

# Introduction

图像修复旨在根据已知区域的结构、纹理和语义信息，对图像中的缺失或被遮挡区域进行合理补全，使生成内容能够与周围上下文自然融合。该任务广泛应用于目标移除、老照片修复、图像编辑和虚拟试穿等场景。随着深度学习的发展，基于 CNN、GAN、Transformer 以及扩散模型的修复方法不断提升生成结果的真实感和多样性。然而，在大面积缺失、不规则掩膜和复杂纹理场景下，现有方法仍容易出现纹理模糊、边界突变、结构断裂或语义不一致等问题。这些问题表明，图像修复不仅需要强大的生成先验，还需要充分利用未掩膜区域中的局部纹理、全局结构和空间位置约束。

近年来，文本到图像扩散模型展现出优秀的图像生成能力，并被进一步扩展到文本引导图像修复任务中。相比传统无条件修复，文本引导修复需要在指定掩膜区域内生成符合用户提示的内容，同时保持修复区域与原图背景在颜色、纹理和结构上的一致性。现有扩散式修复方法大致可以分为两类：一类是在采样过程中对已知区域和未知区域进行融合，具有较好的即插即用性，但由于缺乏对掩膜边界和上下文结构的显式感知，生成结果往往不够连贯；另一类是针对修复任务重新训练专用扩散模型，将带噪潜变量、掩膜图像潜变量、掩膜和文本条件在网络早期进行融合，虽然能够增强内容感知能力，但也会使掩膜图像特征过早受到文本语义干扰，并增加主干 UNet 同时承担条件提取和生成去噪的负担。

BrushNet 等双分支扩散修复框架通过将掩膜图像特征和带噪生成潜变量分离建模，为上述问题提供了新的思路。辅助分支专门负责提取掩膜图像中的像素级内容先验，再以层级方式注入冻结的预训练扩散主干，从而兼顾即插即用能力和形状、内容约束。然而，简单的层级特征注入仍存在两个不足：一方面，不同通道对应的纹理、边缘、颜色和语义响应对修复任务的重要性并不相同；另一方面，掩膜内部、掩膜边界和远离缺失区域的位置也应具有不同的关注强度。若对所有通道和空间位置采用统一的融合权重，模型难以针对复杂纹理、细长结构和边界过渡区域进行自适应增强。

与此同时，已有纹理感知和动态注意力修复方法表明，多尺度特征融合、上下文注意力以及局部-全局信息协同对提升修复质量具有重要作用。纹理先验能够缓解大面积缺失区域中的模糊和重复伪影，上下文注意力有助于捕获远距离依赖关系，而局部细节与全局结构的联合建模可以提高复杂场景下的语义完整性。受这些思想启发，本文认为，在扩散修复网络中进一步增强多尺度条件特征的通道选择能力、空间显著性建模能力和残差稳定性，是提升修复区域真实感与边界协调性的关键。

此外，文本引导图像修复还面临提示语义与视觉合理性之间的平衡问题。仅依赖训练阶段学习到的扩散先验，模型可能生成局部视觉上合理但与文本提示不一致的内容；而过强的文本约束又可能破坏已知区域上下文，导致风格不连续或边界突兀。FreeInpaint 等推理阶段优化方法说明，在不重新训练模型的情况下，利用可微奖励对扩散采样过程进行引导，可以有效改善提示对齐和视觉质量。然而，来自不同预训练评价模型的奖励在数值量纲和梯度幅值上存在差异，直接加权求和可能使某一奖励主导潜变量的更新方向。因此，如何在兼顾全局图文偏好、掩膜区域局部语义和修复视觉合理性的同时，稳定不同奖励对采样轨迹的相对影响，仍是值得研究的问题。

针对上述问题，本文提出一种融合自适应残差注意力与三元奖励引导的文本引导图像修复方法。整体框架以双分支潜空间扩散修复网络为基础：主干分支继承预训练扩散模型的生成能力，辅助分支以带噪潜变量、掩膜图像潜变量和下采样掩膜为输入，提取纯视觉条件特征，并通过逐层、逐尺度的方式注入主干网络。在此基础上，本文设计自适应残差注意力融合模块（Adaptive Residual Attention Fusion Module, ARAF），从通道和空间两个维度对多尺度条件特征进行重标定，并利用可学习残差权重平衡注意力增强特征与原始特征，从而提升训练稳定性和特征融合鲁棒性。进一步地，本文构建 ARAF-UNet，将该模块系统性嵌入下采样层、中间瓶颈层和上采样层，使网络能够同时强化浅层纹理边界、深层语义结构以及解码阶段的细节恢复。

在推理阶段，本文进一步提出三元奖励引导模块（Tri-reward Guidance, TriRG），通过全局图文偏好奖励、掩膜区域局部语义奖励和修复质量奖励共同修正扩散去噪方向。针对三类奖励来源不同、梯度幅值不一致的问题，TriRG 分别计算每项奖励相对于当前潜变量的梯度，再根据梯度均方根幅值进行自适应重标定和加权融合，以避免单一奖励因数值尺度较大而过度左右采样轨迹。该模块不改变网络结构，也不引入额外训练，而是在采样阶段利用均衡后的联合奖励梯度校正噪声预测，使生成轨迹兼顾整体提示对齐、掩膜区域语义匹配和修复结果的视觉合理性。

本文的主要贡献如下：

1. 提出一种面向文本引导图像修复的双分支潜空间扩散框架，通过分离掩膜图像条件提取与扩散去噪生成过程，缓解单分支早期融合带来的条件混叠和主干负担问题。
2. 设计自适应残差注意力融合模块 ARAF，并将其嵌入 ARAF-UNet 的多尺度特征流中，实现通道重要性、空间显著性和残差稳定性的联合建模，从而增强修复区域及其边界处的特征表达。
3. 提出推理阶段的三元奖励引导模块 TriRG，联合全局图文偏好、掩膜区域局部语义和修复视觉合理性三类奖励，并通过分奖励梯度均衡缓解异构奖励之间的幅值失衡，在不引入额外训练的条件下对采样轨迹进行在线校正。

# Realted Work

## 传统图像修复方法

早期图像修复方法主要依赖手工设计的规则和图像低层统计特性，典型代表包括基于偏微分方程的扩散方法和基于样例块匹配的方法。前者通过从已知区域向缺失区域传播颜色或梯度信息来完成小范围破损修复，能够在细小划痕、局部噪声等简单场景中获得较平滑的结果；后者则从图像未损坏区域中搜索相似纹理块，并将其复制或融合到缺失区域，从而更适合处理重复纹理和局部结构延展问题。然而，这类方法通常依赖局部相似性假设，难以理解图像的高层语义。当缺失区域较大、结构复杂或可参考纹理不足时，传统方法容易产生错误传播、重复伪影和结构不连续等问题。因此，如何在保持局部纹理一致性的同时建模全局语义关系，成为后续深度学习方法关注的核心问题。

## 基于 CNN 和 GAN 的图像修复方法

随着深度学习的发展，基于 CNN 的编码器-解码器结构被广泛用于图像修复任务。此类方法通过多层卷积提取层级特征，并利用解码器重建缺失内容，相比传统方法具备更强的语义表达能力。为进一步适应不规则掩膜，一些方法引入部分卷积、门控卷积和多尺度跳跃连接等机制，使网络能够区分已知区域与缺失区域，并在不同尺度上传递纹理和结构信息。多尺度特征融合方法能够缓解浅层细节与深层语义之间的差异，在边界过渡和纹理恢复方面具有一定优势。

GAN 的引入进一步提升了修复结果的真实感。生成器负责合成缺失区域，判别器则从全局或局部角度约束生成内容的视觉分布，使结果更加符合自然图像统计规律。部分方法还结合局部判别器、结构先验或纹理先验，以增强人脸、建筑和场景图像中的结构一致性。然而，CNN 的有效感受野有限，难以充分捕获远距离依赖；GAN 训练过程也存在不稳定、模式崩溃和细节伪影等问题。尤其在大面积自由形状掩膜或复杂背景下，仅依赖卷积局部建模和对抗损失往往难以同时保证语义合理性、纹理连续性和边界自然性。

## 基于注意力和 Transformer 的图像修复方法

为增强长距离依赖建模能力，注意力机制被逐渐引入图像修复任务。上下文注意力通过在已知区域中搜索与缺失区域相似的特征响应，将远处可参考纹理显式传递到待修复位置，从而改善大面积缺失区域中的结构和纹理一致性。后续方法进一步探索双向注意力、多尺度注意力和语义感知注意力，以缓解单向信息传播不足、复杂语义混合以及特征层级不匹配等问题。

Transformer 结构由于具备全局关系建模能力，也被用于图像修复。与卷积方法相比，Transformer 能够在更大范围内建立像素或图像块之间的依赖关系，有助于恢复物体轮廓、场景布局和重复纹理。Diff-GDAformer 等方法将扩散模型生成的先验特征与动态注意力 Transformer 结合，通过局部自注意力捕获细粒度细节，通过递归泛化注意力聚合全局上下文，并利用混合特征融合模块加强局部与全局信息交互。类似地，层次化纹理感知修复方法利用 Gabor 纹理先验、多尺度密集跳跃连接和上下文注意力，引导网络在纹理恢复与结构补全之间取得平衡。这些研究表明，纹理先验、多尺度融合和局部-全局协同建模对复杂图像修复具有重要意义。但 Transformer 和复杂注意力模块通常带来较高计算开销，且在文本引导扩散修复场景中，如何将这些能力以轻量、稳定的方式融入多尺度去噪网络仍有进一步研究空间。

## 基于扩散模型的图像修复方法

扩散模型通过逐步去噪过程学习图像数据分布，在高质量图像生成和文本到图像生成任务中取得了显著效果，也推动了图像修复方法的发展。早期扩散式修复方法通常直接修改采样策略，在反向扩散过程中保留已知区域，并仅对掩膜区域进行生成。例如，RePaint 和 Blended Latent Diffusion 等方法能够较方便地适配预训练扩散模型，具有较好的灵活性。然而，由于这类方法主要依靠采样阶段的区域融合，对掩膜边界、已知区域上下文和文本语义缺乏充分建模，复杂掩膜下容易出现内容不连贯和边界不自然的问题。

另一类方法通过重新训练或微调专用图像修复扩散模型来提升任务适应性。Stable Diffusion Inpainting、PowerPaint 和 HD-Painter 等方法将掩膜、掩膜图像和文本条件共同输入去噪网络，使模型具备更强的内容感知和形状感知能力。但这类单分支早期融合结构也存在一定局限：带噪潜变量、掩膜图像潜变量、掩膜和文本条件在网络初始阶段混合，容易使掩膜图像特征受到文本条件干扰；同时，主干 UNet 需要同时承担条件提取和图像生成任务，增加了学习负担，并降低了向不同预训练扩散模型迁移的便利性。

为解决上述问题，BrushNet 提出将掩膜图像特征提取与带噪潜变量去噪分离到两个分支中，通过额外分支提取纯视觉条件特征，并以逐层、逐尺度的方式注入冻结扩散主干。这种双分支结构降低了主干网络的学习压力，也增强了对掩膜形状和未掩膜内容的感知能力。本文方法继承这一思路，但进一步关注条件特征注入过程中的自适应选择问题：不同尺度、通道和空间位置对修复质量的贡献并不相同，因此有必要在多尺度特征流中引入轻量注意力和残差融合机制，以增强关键纹理、边界和语义响应。

## 推理阶段引导与文本对齐方法

除网络结构设计外，推理阶段的采样引导也是提升文本引导修复质量的重要方向。文本引导图像修复不仅要求生成区域符合用户提示，还要求其与原图上下文保持视觉协调。已有研究表明，扩散模型对初始噪声和中间潜变量较为敏感，采样轨迹的细微变化可能导致生成对象缺失、语义偏移或局部视觉不合理。FreeInpaint 从这一角度出发，在不重新训练模型的条件下优化初始噪声，并设计面向修复任务的训练无关引导目标，以改善提示对齐和视觉合理性。

通用文本到图像任务中的分类器引导、CLIP 引导和注意力引导为推理阶段优化提供了基础，但直接用于图像修复并不充分。图像修复不仅需要整幅图像与文本描述保持一致，还需关注掩膜区域内部的局部语义与生成内容的修复质量。FreeInpaint 通过多种可微奖励为这一问题提供了有效的推理引导思路，但异构奖励直接求和时容易受数值范围和梯度幅值差异影响。本文提出的 TriRG 在三奖励引导基础上对各项奖励梯度进行独立估计和幅值均衡，从而在保持双分支扩散模型生成先验的同时，更稳定地兼顾全局图文偏好、掩膜区域语义匹配和修复视觉合理性。

# Method

## 网络结构设计

为充分建模掩膜区域与未掩膜区域之间的结构约束和语义关联，本文构建了一种融合自适应残差注意力与三元奖励引导的双分支潜空间扩散修复网络，记为 ARAF-UNet。整体上，网络由冻结的预训练扩散去噪主干、辅助条件分支、自适应残差注意力融合模块（Adaptive Residual Attention Fusion Module, ARAF）以及推理阶段三元奖励引导模块（Tri-reward Guidance, TriRG）组成。其中，去噪主干继承文本到图像扩散模型的生成先验，负责在文本条件约束下预测噪声残差；辅助条件分支负责从掩膜图像中提取纯视觉内容先验；ARAF 对辅助条件分支产生的多尺度条件特征进行通道、空间和残差融合重标定；TriRG 则在采样阶段联合三类修复奖励，并通过分奖励梯度均衡后的联合梯度对去噪方向进行在线校正。

给定输入图像 $x$ 与二值掩膜 $m$，首先根据掩膜得到掩膜图像 $x_m$，并利用 VAE 编码器将其映射到潜空间，得到掩膜图像潜变量 $z_m$。与此同时，原始图像在扩散过程中被加噪得到当前时刻的噪声潜变量 $z_t$，掩膜 $m$ 经插值下采样后得到与潜空间分辨率一致的 $\tilde m$。随后，将 $z_t$、$z_m$ 与 $\tilde m$ 在通道维拼接，构成辅助分支的输入：

$$
z_{in}=[z_t,z_m,\tilde m].
$$

其中，$z_t$ 提供当前去噪阶段的生成状态信息，$z_m$ 提供来自已知区域的外观与结构先验，$\tilde m$ 则显式指示待编辑区域的位置分布。这样的输入组织方式使网络能够同时感知“当前生成状态”“可参考内容”与“空间约束区域”。

辅助分支在结构上采用与扩散主干同尺度的编码器-解码器框架，但移除了文本交叉注意力，仅保留纯视觉特征建模路径。这样做的目的是避免文本语义过早干扰掩膜图像特征提取，使辅助分支更加专注于建模图像本身的纹理、颜色和边界信息。进一步地，辅助分支各层输出并不是一次性注入主干，而是通过零初始化的 $1\times1$ 卷积以逐层、逐尺度的方式加到主干分支的下采样层、中间瓶颈层和上采样层特征上，即

$$
F_i' = F_i + \lambda \, Z(B_i),
$$

其中，$F_i$ 表示主干分支第 $i$ 层特征，$B_i$ 表示辅助分支对应层特征，$Z(\cdot)$ 为零初始化卷积映射，$\lambda$ 为条件控制强度。该层级注入策略能够在不同分辨率下持续提供像素级约束，相比仅在浅层或末端进行条件融合，更有利于边界过渡、结构保持和语义一致性。

## 自适应残差注意力融合模块

3.2 自适应残差注意力融合模块

为了增强多尺度特征在修复区域与结构边界处的表达能力，受 CBAM 和 SENet 的启发，本章提出自适应残差注意力融合模块（Adaptive Residual Attention Fusion Module, ARAF）。该模块被引入 ARAF-UNet 的多尺度分支中，通过通道重标定、空间显著性建模以及可学习残差融合，对中间特征进行逐级细化。相比直接替换原始特征，ARAF 在增强判别性特征的同时保留原始特征流的稳定传递，从而更适合扩散模型中 ARAF-UNet 的多尺度特征交互场景。

设输入特征为 $X∈R^{C×H×W}$。ARAF 首先执行通道注意力建模，通过全局平均池化将空间维压缩为通道描述子

$z_c=\frac{1}{H×W}∑_{i=1}^H∑_{j=1}^WX_c(i,j),$

其中 $z∈R^C$ 表征各通道的全局响应。随后，利用两层 $1×1$ 卷积构成的瓶颈映射对通道依赖关系进行显式建模，中间通道维度按压缩率 $r$ 降维，再经非线性激活后恢复到原通道数，并通过 Sigmoid 得到通道权重：

$M_c(X)=σ(W_2(δ(W_1(z)))),$

其中 $W_1∈R^{\frac{C}{r}×C}$，$W_2∈R^{C×\frac{C}{r}}$，$δ(⋅)$ 为非线性激活函数，$σ(⋅)$ 为 Sigmoid 函数。最终获得通道增强特征

$X_c=M_c(X)⊙X,$

其中 $⊙$ 表示逐元素乘法。该过程能够依据全局上下文自适应地提升关键语义通道响应，抑制冗余或弱相关通道。

在此基础上，本文进一步对通道增强后的特征执行空间显著性建模。具体地，在通道维上分别施加平均池化和最大池化，得到两个空间描述图：

$A_{avg}=AvgPool_{ch}(X_c),A_{max}=MaxPool_{ch}(X_c),$

其中 $A_{avg},A_{max}∈R^{1×H×W}$。随后将二者在通道维拼接，并通过一个 $7×7$ 卷积生成空间注意力图：

$M_s(X_c)=σ(f^{7×7}([A_{avg};A_{max}])).$

最终得到经过空间重加权后的注意力特征

$X_a=M_s(X_c)⊙X_c.$

该设计能够在“何处需要关注”的层面进一步突出边缘、纹理和待修复区域等关键空间位置，与通道注意力形成互补。

考虑到多尺度扩散网络在训练初期对特征扰动较为敏感，本文没有直接以注意力输出替代原始特征，而是引入可学习的残差融合机制：

$α=σ(θ),Y=αX_a+(1-α)X,$

其中 $θ$ 为可训练标量参数，$α∈(0,1)$ 表示注意力增强特征与原始特征之间的自适应融合权重，$Y$ 为模块输出。该策略使网络能够在训练过程中自动平衡“注意力增强”与“原始信息保留”之间的关系：当某一尺度的注意力特征更具判别性时，模型可增大 $α$；反之则保留更多基础特征，从而提升训练稳定性与特征融合鲁棒性。

在结构嵌入方面，ARAF 被系统性插入到 ARAF-UNet 的多尺度特征生成路径中，包括下采样分支、中间瓶颈分支和上采样分支。也就是说，在每个尺度的辅助条件特征经过对应卷积映射后，均先通过 ARAF 完成通道与空间的联合重标定，再参与后续的条件控制与残差输出。这样的设计使得模块不仅能够在浅层强化局部纹理与边界信息，还能够在深层增强高层语义与全局结构一致性，从而提升辅助条件特征向主干 UNet 注入时的有效性。由于该模块仅包含全局池化、轻量瓶颈映射、单层 $7×7$ 卷积以及逐元素加权运算，因此额外参数量和计算开销较小，适合在多尺度扩散框架中逐层部署。

总体而言，本文提出的 ARAF 通过通道关系建模、空间位置选择和可学习残差融合，实现了面向 ARAF-UNet 多尺度结构的轻量级、自适应特征增强。该模块能够从“通道重要性”和“空间显著性”两个维度协同提升条件特征质量，并在保留原始特征表达稳定性的同时增强关键区域响应，为后续图像修复与细节重建提供更加判别且稳定的特征表征。

## 面向图像修复的高效U型网络 ARAF-UNet

3.3 面向图像修复的高效U型网络

在自适应残差注意力融合模块的基础上，本文进一步构建了一种面向图像修复任务的高效 U 型网络，记为 ARAF-UNet。该网络以扩散模型中的 UNet 去噪主干为基础，引入轻量级的自适应残差注意力融合机制，在保持原有编码器-解码器多尺度建模能力的同时，增强网络对掩膜区域、边界过渡区域以及上下文结构信息的感知能力。与直接增加大规模卷积层或 Transformer 模块不同，ARAF-UNet 通过在关键尺度位置插入 ARAF 模块实现特征重标定，因此能够以较小的参数增量提升修复质量，适合部署在高分辨率图像修复与文本引导图像修复场景中。

整体结构上，ARAF-UNet 仍然遵循典型的 U 型编码器-瓶颈-解码器范式。编码阶段逐步降低特征图分辨率并扩大感受野，用于提取由局部纹理到全局语义的层级表示；瓶颈阶段在最低分辨率下建模全局结构关系；解码阶段则逐步恢复空间分辨率，并结合跳跃连接补充细粒度边缘与纹理信息。设第 $i$ 个尺度上的 UNet 特征为 $F_i$，ARAF-UNet 不改变原始特征流的主干拓扑，而是在各尺度特征输出后引入 ARAF 模块，对其进行通道和空间两个维度的自适应增强：

$$
\hat F_i = \operatorname{ARAF}_i(F_i).
$$

其中，$\operatorname{ARAF}_i(\cdot)$ 表示第 $i$ 个尺度对应的自适应残差注意力融合单元。该单元先通过通道注意力选择对当前修复任务更重要的语义响应，再通过空间注意力突出掩膜内部、边界邻域和结构延展区域，最后利用可学习残差权重在注意力增强特征与原始特征之间进行平衡。因此，ARAF-UNet 在强化判别性区域响应的同时，不会破坏扩散 UNet 已有的稳定去噪表征。

在具体插入方式上，ARAF-UNet 采用多位置部署策略，将 ARAF 分别插入 UNet 的下采样分支、中间瓶颈分支和上采样分支。首先，在下采样阶段，每个编码块输出特征后接入一个 ARAF 模块，即

$$
F_i^{down} = \operatorname{ARAF}_i^{down}(B_i^{down}(F_{i-1}^{down})),
$$

其中 $B_i^{down}$ 表示第 $i$ 个下采样块。该设计使网络在浅层即可关注缺失区域附近的纹理、边缘和颜色分布，并在深层逐步强化结构语义信息。对于图像修复任务而言，下采样分支承担着从可见区域提取上下文先验的重要作用，因此在该阶段加入 ARAF 有助于减少由掩膜形状不规则或缺失面积过大带来的结构断裂问题。

其次，在 UNet 的中间瓶颈层插入一个独立的 ARAF 模块：

$$
F^{mid} = \operatorname{ARAF}^{mid}(B^{mid}(F^{down})).
$$

瓶颈层具有最大的感受野，能够聚合全局语义信息。此处的 ARAF 模块主要用于筛选与修复目标相关的全局结构响应，例如物体轮廓、场景布局和语义一致性约束，从而为后续解码阶段提供更加稳定的全局指导。相比只在浅层增强局部纹理，在瓶颈层引入 ARAF 能够进一步提升大面积缺失区域中的结构完整性。

最后，在上采样阶段，每个解码块输出后同样接入 ARAF 模块：

$$
F_i^{up} = \operatorname{ARAF}_i^{up}(B_i^{up}([F_{i+1}^{up}, F_i^{skip}])).
$$

其中 $F_i^{skip}$ 表示来自编码器对应尺度的跳跃连接特征，$[\cdot,\cdot]$ 表示特征拼接。上采样分支直接影响最终图像的细节恢复质量，因此在该阶段引入 ARAF 可以进一步强化边界过渡、纹理连续性和局部视觉一致性。特别是在掩膜边缘区域，空间注意力能够自适应提高边界邻域的权重，使生成内容与已知区域之间的融合更加自然。

从功能角度看，ARAF-UNet 的核心优势体现在三个方面。第一，网络保留了 UNet 原有的多尺度跳跃连接结构，能够同时利用浅层细节和深层语义，保证修复结果的结构完整性。第二，ARAF 模块采用轻量级通道注意力和空间注意力组合，只引入少量额外参数，避免了高分辨率修复中常见的显存和计算开销过大问题。第三，可学习残差融合机制使网络能够根据不同尺度、不同区域和不同训练阶段动态调整注意力增强强度，从而提升训练稳定性和跨场景泛化能力。

对于扩散式图像修复任务，ARAF-UNet 可以被视为一种增强后的去噪预测网络。在给定噪声潜变量 $z_t$、文本条件 $c$、掩膜图像条件 $I_m$ 和掩膜 $M$ 时，网络预测噪声残差可表示为

$$
\epsilon_\theta = \operatorname{ARAF\text{-}UNet}(z_t,t,c,I_m,M).
$$

由于 ARAF 被嵌入到 UNet 的多尺度特征流中，网络在每个去噪阶段都能够根据当前潜变量状态自适应地调整特征响应。这样既可以利用扩散模型的生成先验保证内容真实感，又可以利用掩膜图像和空间注意力约束提高局部修复准确性。尤其是在复杂背景、细长结构、纹理重复区域和大面积缺失场景中，ARAF-UNet 能够更好地保持修复区域与上下文之间的语义连贯和视觉一致。

综上，ARAF-UNet 并不是对原始 UNet 的大规模重构，而是在其关键多尺度节点上引入自适应残差注意力融合模块，形成一种轻量、稳定且面向修复任务优化的增强型 U 型网络。该设计将 UNet 的层级表征能力、跳跃连接的细节保留能力以及 ARAF 的区域自适应增强能力结合起来，为后续的 TriRG 推理引导和扩散损失训练提供了更高质量的基础特征表示。


## 三元奖励引导模块-TriRG

为进一步提升扩散修复过程中生成内容的提示对齐度、局部语义准确性与修复视觉合理性，本文在扩散采样阶段引入三元奖励引导模块（Tri-reward Guidance, TriRG）。TriRG 在 FreeInpaint 多奖励推理引导的基础上，联合全局图文偏好、掩膜区域局部语义和修复质量三类可微奖励，并对不同奖励产生的潜变量梯度进行幅值均衡。该模块不改变扩散模型的网络结构，也不需要对生成模型或奖励模型进行额外训练，而是在采样阶段使用均衡后的联合奖励梯度校正去噪方向。

具体而言，给定第 $t$ 个去噪时刻的潜变量 $z_t$、文本条件 $c$、掩膜图像条件 $z_m$ 以及下采样后的掩膜 $\tilde m$，扩散去噪网络首先预测当前潜变量的噪声残差：

$$
\epsilon_\theta = \epsilon_\theta(z_t,t,c,z_m,\tilde m).
$$

随后，根据扩散调度器的参数化形式，将当前潜变量对应的中间重建结果通过 VAE 解码器映射回图像空间，得到当前时刻的可微图像估计 $x_t$。设文本提示为 $c$，待修复区域的二值掩膜为 $m$，TriRG 在 $x_t$ 上计算以下三类奖励。

第一项为全局图文偏好奖励 $R_{\mathrm{img}}$。本文使用 ImageReward 对整幅中间图像与输入提示进行评估：

$$
R_{\mathrm{img}} = f_{\mathrm{IR}}(x_t,c),
$$

其中 $f_{\mathrm{IR}}$ 表示预训练的 ImageReward 模型。该奖励从整体层面评估图像内容与文本描述的匹配程度和视觉偏好，用于维持生成结果对完整文本提示的响应。

第二项为掩膜区域局部语义奖励 $R_{\mathrm{loc}}$。具体地，使用掩膜保留当前图像估计中的待修复区域，并计算该区域与同一输入文本提示的 CLIP 语义匹配度：

$$
R_{\mathrm{loc}}
=
-d_{\mathrm{sph}}
\left(
E_I(x_t\odot m),E_T(c)
\right),
$$

其中 $E_I$ 和 $E_T$ 分别表示 CLIP 的图像编码器和文本编码器，$d_{\mathrm{sph}}$ 表示球面距离。该奖励通过抑制背景区域对图文相似度的干扰，强化输入提示对掩膜区域生成内容的直接约束。

第三项为修复质量奖励 $R_{\mathrm{inp}}$。本文将当前图像估计和修复掩膜共同输入预训练的 InpaintReward 模型：

$$
R_{\mathrm{inp}}=f_{\mathrm{IP}}(x_t,m),
$$

其中 $f_{\mathrm{IP}}$ 表示针对图像修复结果训练的偏好评估模型。该奖励提供与修复任务相关的整体视觉质量信号，与前两项图文奖励形成互补。

现有多奖励引导通常先对上述三项奖励进行标量加权求和，再对联合奖励求梯度。然而，ImageReward、CLIP 相似度和 InpaintReward 来自不同的预训练模型，其输出范围和对潜变量的梯度幅值不具有直接可比性。在固定奖励权重下，梯度较大的奖励可能过度主导去噪方向，导致其他约束的作用被削弱。

为此，TriRG 采用分奖励梯度均衡。对于奖励 $R_k\in\{R_{\mathrm{img}},R_{\mathrm{loc}},R_{\mathrm{inp}}\}$，首先分别计算其关于当前潜变量的梯度：

$$
g_t^{(k)}=\nabla_{z_t}R_k.
$$

设单个样本的潜变量梯度共包含 $D$ 个元素，该梯度的均方根幅值定义为：

$$
n_t^{(k)}
=
\max\left(
\sqrt{
\frac{1}{D}
\left\|g_t^{(k)}\right\|_2^2
},
\varepsilon
\right).
$$

然后以三项梯度幅值的平均值作为当前采样步的参考尺度：

$$
\bar n_t
=
\frac{1}{3}
\sum_{k\in\{\mathrm{img},\mathrm{loc},\mathrm{inp}\}}
n_t^{(k)}.
$$

对第 $k$ 项奖励梯度进行自适应重标定：

$$
\tilde g_t^{(k)}
=
\operatorname{clip}
\left(
\frac{\bar n_t}{n_t^{(k)}},
r_{\min},r_{\max}
\right)
g_t^{(k)},
$$

其中 $\varepsilon$ 用于避免数值不稳定，$r_{\min}$ 和 $r_{\max}$ 用于限制梯度缩放比例，防止极小梯度被过度放大。在完成幅值均衡后，三项梯度按预设奖励权重进行联合：

$$
g_t
=
\lambda_g
\left(
\lambda_{\mathrm{img}}\tilde g_t^{(\mathrm{img})}
+\lambda_{\mathrm{loc}}\tilde g_t^{(\mathrm{loc})}
+\lambda_{\mathrm{inp}}\tilde g_t^{(\mathrm{inp})}
\right).
$$

其中 $\lambda_{\mathrm{img}}$、$\lambda_{\mathrm{loc}}$ 和 $\lambda_{\mathrm{inp}}$ 分别表示全局图文偏好奖励、掩膜区域局部语义奖励和修复质量奖励的相对权重，$\lambda_g$ 表示总体奖励引导强度。梯度均衡仅调整不同奖励的实际梯度幅值，预设权重仍然保留了对三类目标相对重要性的显式控制。

在本文采用的 UniPC 调度器下，均衡后的联合梯度用于修正原始噪声预测：

$$
\hat{\epsilon}_t
=
\epsilon_\theta(z_t,t,c,z_m,\tilde m)
-\sqrt{\bar\alpha_t}\,g_t,
$$

其中 $\bar\alpha_t$ 为扩散调度器在时刻 $t$ 的累积信号保留系数。修正后的噪声预测 $\hat\epsilon_t$ 参与后续采样更新，使潜变量的演化方向同时受到整体图文偏好、掩膜区域语义和修复质量约束，并避免单一奖励因梯度幅值较大而压制其他目标。

考虑到分别计算三项奖励梯度会增加推理开销，本文沿用间隔式引导策略，仅每隔 $K$ 个去噪步执行一次 TriRG 更新。该策略减少了相对于逐步奖励引导的额外计算成本，同时保留了 TriRG 作为无需重新训练的推理插件集成到扩散修复流程中的便利性。

## 损失函数

3.4 训练与推理策略

在训练阶段，本文采用潜空间扩散模型的噪声预测目标对条件去噪网络进行监督。给定原始图像经变分自编码器编码得到的潜变量 $z_0$，从标准高斯分布中采样噪声 $\epsilon \sim \mathcal{N}(0,I)$，并随机采样扩散时间步 $t$。前向加噪过程可表示为：

$$
z_t=\sqrt{\bar{\alpha}_t}z_0+\sqrt{1-\bar{\alpha}_t}\epsilon,
$$

其中，$\bar{\alpha}_t$ 表示噪声调度器在第 $t$ 个时间步对应的累计噪声系数。条件去噪网络以带噪潜变量 $z_t$、时间步 $t$、文本语义条件 $c$、掩膜图像潜变量 $z_m$ 以及与潜空间分辨率对齐后的二值掩膜 $m$ 为输入，预测前向过程中加入的噪声残差：

$$
\hat{\epsilon}=\epsilon_\theta(z_t,t,c,z_m,m).
$$

因此，扩散训练损失定义为预测噪声与真实噪声之间的均方误差：

$$
\mathcal{L}_{\mathrm{diff}}
=
\mathbb{E}_{z_0,\epsilon,t,c,z_m,m}
\left[
\left\|
\epsilon-\epsilon_\theta(z_t,t,c,z_m,m)
\right\|_2^2
\right].
$$

该目标使模型在不同噪声强度下学习由文本、已知图像区域和修复掩膜共同约束的去噪方向。自适应残差注意力融合模块在上述损失监督下与条件分支端到端联合优化，其产生的多尺度残差特征通过加性方式注入去噪主干，使梯度能够由噪声预测误差直接回传至各尺度融合权重。推理阶段的 TriRG 模块不参与参数学习，而是作为训练无关的采样引导策略嵌入反向扩散过程。由此，本文方法兼具结构级建模能力与采样级优化能力：前者通过增强条件注入过程提升多尺度特征表达，后者通过带有分奖励梯度均衡的三元奖励引导进一步修正生成方向，以增强自由形状图像修复的文本对齐、掩膜区域语义匹配和视觉合理性。

# Experiments

## 实验设置

训练过程中，图像分辨率设为 $512\times512$，学习率设为 $1\times10^{-5}$，优化器采用 AdamW。由于本文实验在单张 NVIDIA GeForce RTX 4090 上完成，批大小根据显存占用设置为 2，并采用混合精度训练以降低显存开销。训练时仅更新 BrushNet 辅助分支及本文新增的 ARAF 模块参数，预训练扩散主干、VAE 和文本编码器保持冻结，从而继承基础扩散模型的生成先验并降低训练成本。软件环境为 Ubuntu 24.04、Python 3.10、PyTorch 1.13.0 和 CUDA 12.3。
