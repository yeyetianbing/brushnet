# Abstract

图像修复的目的是用视觉上连贯的和语义上可信的内容来修复图像中缺失的区域。虽然深度学习方法已经取得了很大的进展，但目前的方法在处理大面积图像修复任务时仍然面临挑战，通常会产生模糊的纹理或结构不一致的结果。这些限制主要源于对远程依赖项的利用不足和纹理先验信息不足。为了解决这些问题，我们提出了一种融合了多方向纹理先验知识和上下文信息的两阶段图像修复框架。在第一阶段，我们利用模拟人类视觉感知的Gabor滤波器从受损图像中提取丰富的纹理特征。然后将这些特征融合以指导纹理修复网络，其中引入了多尺度密集跳过连接(MSDSC)模块来弥合不同特征级别之间的语义差距。在第二阶段，我们设计了一个层次化的纹理感知引导图像完成网络，该网络利用修复后的纹理作为辅助引导。具体地说，加入了上下文注意模块，以捕获远程空间依赖关系并增强结构一致性。在CelebA-HQ、Places2和Paris Street View等三个具有挑战性的基准上进行的广泛实验表明，我们的方法在量化指标和视觉质量方面都优于现有的最先进方法。该框架显著提高了修复结果的真实感和连贯性，特别是对于具有较大缺失区域或复杂纹理的图像。

文本引导的图像修复致力于使用用户的文本提示在图像的指定区域内生成新内容。主要的挑战是在保持高度视觉保真度的同时，准确地将内画区域与用户提供的提示对齐。虽然现有的修复方法已经通过利用预先训练的文本到图像扩散模型产生了视觉上令人信服的结果，但它们仍然难以同时保持即时对齐和视觉合理性。

在这项工作中，我们引入了FreeInaint，这是一种即插即用的免调整方法，它在推理过程中直接动态优化扩散潜伏期，以提高生成图像的真实性。

此外，我们还精心设计了专门为修复任务量身定做的复合制导目标。这个目标有效地指导去噪过程，通过优化每个步骤的中间潜伏期来增强即时对准和视觉合理性。通过涉及各种修复扩散模型和评估指标的大量实验，我们展示了我们所提出的Free Inaint的有效性和健壮性。

# Introduction

近年来见证了大文本到图像(T2I)扩散模型的成功……

扩展用于文本引导图像修复的预训练T2I扩散模型的方法……

为了同时实现快速对齐和视觉合理性，我们的研究深入到反向扩散过程中扩散潜伏期

的直接优化。值得注意的是，我们认为在推理过程中应该考虑两个关键因素。……另一种是在整个去噪过程中提供特定任务的指导。虽然利用T2I模型的生成能力在各种下游任务中被证明是有效的，但从不同的训练数据派生的生成数据分布经常偏离个别任务的特定要求。具体地说，对于图像修复任务，这种差异会降低生成图像的视觉合理性。事实证明，通过利用可区分的奖励来优化推理过程中的扩散潜伏期，免培训指导在将生成的图像与特定任务的目标对齐方面是有效的(Dhariwal和Nichol2021；Chefer等人。2023年)。然而，为修复任务量身定做的有效指导目标仍未得到探索。

通过在去噪过程中同时优化初始噪声和中间延迟的思想，我们新颖地提出了……

一方面，

另一方面，我们精心制作了一个分解的免训练指导目标，称为DEGU，为修复任务定制以指导去噪过程。具体地说，我们将修复过程的条件分布分解为三个不同的目标：文本对齐、视觉合理性和人类偏好。这样的分解使现有的可区分模型无缝集成，从而全面提升修复模型的感知和生成质量，而不需要任何培训或微调要求。

这项工作的主要贡献是提出了一种自由调谐的方法，通过直接优化推理过程中的扩散延迟，简化了文本引导的图像修复任务。我们的方法巧妙地探索了如何利用为图像修复量身定做的先验知识来优化初始噪声，以及如何在特定任务的指导下改进去噪过程。通过在EditBitch和MSCOCO数据集上使用一系列修复扩散模型进行全面的实验，我们证明了Free Inaint的有效性和健壮性。

# Realted Work

图像修复传统的方法

图像修复CNN和GAN的方法

图像修复Transformer的方法

图像修复扩散模型的方法

# Method

## 正文

为提升自由形状掩膜图像修复任务中的结构一致性、语义对齐能力与细节生成质量，本文在 BrushNet 框架基础上，引入自适应残差注意力融合模块和 DeGu 引导机制，构建了一种兼顾多尺度特征建模与推理阶段语义校正的图像修复方法。整体上，所提出模型仍继承 BrushNet 的双分支扩散修复范式：主干扩散模型负责逐步去噪生成，BrushNet 分支则从掩膜图像中提取条件特征，并以分层方式注入到去噪网络中，从而增强待修复区域与已知上下文之间的结构关联。

设原始图像为 $I∈R^{H×W×3}$，二值掩膜为 $M∈{0,1}^{H×W}$，其中特征缺失区域由 $M=1$ 表示，掩膜后的输入图像记为 $I_m=I⊙(1-M)$。在扩散去噪过程中，BrushNet 将噪声潜变量与掩膜图像特征进行解耦建模，分别承担生成先验建模与条件约束两类功能。与原始 BrushNet 不同，本文进一步在 BrushNet 分支的多尺度特征传递过程中加入自适应残差注意力融合模块，使网络能够对不同通道和空间位置的重要性进行动态重标定；同时，在推理阶段引入 FreeInpaint 中的 DeGu 机制，对每一步去噪结果进行分解式引导，以提升语义一致性与视觉合理性。

## 主干

3.1 基于 BrushNet 的双分支修复框架

BrushNet 的核心思想是将掩膜图像特征与扩散噪声潜变量分离建模，从而减轻单一 U-Net 同时学习结构保持、语义生成与边界协调的负担。具体而言，主扩散分支接收带噪潜变量 $z_t$ 及文本条件 $c$，用于预测当前时刻的噪声；BrushNet 条件分支则以掩膜图像和掩膜信息为输入，提取与修复区域相关的层级特征，并在下采样、中间层和上采样阶段逐层注入主干网络。该设计使模型在生成缺失内容时，能够充分利用未掩膜区域的纹理、边缘和语义上下文信息，为后续细节恢复提供稳定条件先验。

## 主干2

为充分建模掩膜区域与未掩膜区域之间的结构约束和语义关联，本文构建了一种双分支潜空间扩散修复网络。整体上，网络由冻结的预训练扩散去噪主干、面向掩膜图像的辅助引导分支、自适应残差注意力融合模块以及 DeGu 引导模块组成。其中，主干分支负责文本条件下的图像生成与逐步去噪，辅助分支负责提取掩膜图像中的内容先验，并以层级方式注入主干网络，从而实现对待修复区域的细粒度约束。

给定输入图像 $x$ 与二值掩膜 $m$，首先根据掩膜得到掩膜图像 $x_m$，并利用 VAE 编码器将其映射到潜空间，得到掩膜图像潜变量 $z_m$。与此同时，原始图像在扩散过程中被加噪得到当前时刻的噪声潜变量 $z_t$，掩膜 $m$ 经插值下采样后得到与潜空间分辨率一致的 $m$。随后，将 $z_t$、$z_m$ 与 $m$ 在通道维拼接，构成辅助分支的输入：

$z_{in}=[z_t,z_m,m].$

其中，$z_t$ 提供当前去噪阶段的生成状态信息，$z_m$ 提供来自已知区域的外观与结构先验，$m$ 则显式指示待编辑区域的位置分布。这样的输入组织方式使网络能够同时感知“当前生成状态”“可参考内容”与“空间约束区域”。

辅助分支在结构上采用与扩散主干同尺度的编码器-解码器框架，但移除了文本交叉注意力，仅保留纯视觉特征建模路径。这样做的目的是避免文本语义过早干扰掩膜图像特征提取，使辅助分支更加专注于建模图像本身的纹理、颜色和边界信息。进一步地，辅助分支各层输出并不是一次性注入主干，而是通过零初始化的 $1×1$ 卷积以逐层、逐尺度的方式加到主干分支的下采样层、中间瓶颈层和上采样层特征上，即

$F_i^{\prime}=F_i+\lambda Z(B_i),$

其中，$F_i$ 表示主干分支第 $i$ 层特征，$B_i$ 表示辅助分支对应层特征，$Z(⋅)$ 为零初始化卷积映射，$λ$ 为条件控制强度。该层级注入策略能够在不同分辨率下持续提供像素级约束，相比仅在浅层或末端进行条件融合，更有利于边界过渡、结构保持和语义一致性。

## 自适应残差注意力融合模块

3.2 自适应残差注意力融合模块

为了增强多尺度特征在修复区域与结构边界处的表达能力，受 CBAM 和 SENet 的启发，本章提出自适应残差注意力融合模块（Adaptive Residual Attention Fusion Module, ARAF）。该模块被引入 BrushNet/UNet 的多尺度分支中，通过通道重标定、空间显著性建模以及可学习残差融合，对中间特征进行逐级细化。相比直接替换原始特征，ARAF 在增强判别性特征的同时保留原始特征流的稳定传递，从而更适合扩散模型中 UNet/BrushNet 的多尺度特征交互场景。

设输入特征为 $X∈R^{C×H×W}$。ARAF 首先执行通道注意力建模，通过全局平均池化将空间维压缩为通道描述子

$z_c=\frac{1}{H×W}∑_{i=1}^H∑_{j=1}^WX_c(i,j),$

其中 $z∈R^C$ 表征各通道的全局响应。随后，利用两层 $1×1$ 卷积构成的瓶颈映射对通道依赖关系进行显式建模，中间通道维度按压缩率 $r$ 降维，再经非线性激活后恢复到原通道数，并通过 Sigmoid 得到通道权重：

$M_c(X)=σ(W_2(δ(W_1(z)))),$

其中 $W_1∈R^{\frac{C}{r}×C}$，$W_2∈R^{C×\frac{C}{r}}$，$δ(⋅)$ 为非线性激活函数，$σ(⋅)$ 为 Sigmoid 函数。最终获得通道增强特征

$X_c=M_c(X)⊙X,$

其中 $⊙$ 表示逐元素乘法。该过程能够依据全局上下文自适应地提升关键语义通道响应，抑制冗余或弱相关通道。

在此基础上，本文进一步对通道增强后的特征执行空间显著性建模。具体地，在通道维上分别施加平均池化和最大池化，得到两个空间描述图：

$A_{avg}=AvgPool_{ch}(X_c),A_{max}=MaxPool_{ch}(X_c),$

其中 $A_{avg},A_{max}∈R^{1×H×W}$。随后将二者在通道维拼接，并通过一个 $7×7$ 卷积生成空间注意力图：

$M_s(X_c)=σ(f^{7×7}([A_{avg};A_{max}])).$

最终得到经过空间重加权后的注意力特征

$X_a=M_s(X_c)⊙X_c.$

该设计能够在“何处需要关注”的层面进一步突出边缘、纹理和待修复区域等关键空间位置，与通道注意力形成互补。

考虑到多尺度扩散网络在训练初期对特征扰动较为敏感，本文没有直接以注意力输出替代原始特征，而是引入可学习的残差融合机制：

$α=σ(θ),Y=αX_a+(1-α)X,$

其中 $θ$ 为可训练标量参数，$α∈(0,1)$ 表示注意力增强特征与原始特征之间的自适应融合权重，$Y$ 为模块输出。该策略使网络能够在训练过程中自动平衡“注意力增强”与“原始信息保留”之间的关系：当某一尺度的注意力特征更具判别性时，模型可增大 $α$；反之则保留更多基础特征，从而提升训练稳定性与特征融合鲁棒性。

在结构嵌入方面，ARAF 被系统性插入到 BrushNet 的多尺度特征生成路径中，包括下采样分支、中间瓶颈分支和上采样分支。也就是说，在每个尺度的 BrushNet 特征经过对应卷积映射后，均先通过 ARAF 完成通道与空间的联合重标定，再参与后续的条件控制与残差输出。这样的设计使得模块不仅能够在浅层强化局部纹理与边界信息，还能够在深层增强高层语义与全局结构一致性，从而提升 BrushNet 向主干 UNet 注入条件特征时的有效性。由于该模块仅包含全局池化、轻量瓶颈映射、单层 $7×7$ 卷积以及逐元素加权运算，因此额外参数量和计算开销较小，适合在多尺度扩散框架中逐层部署。

总体而言，本文提出的 ARAF 通过通道关系建模、空间位置选择和可学习残差融合，实现了面向 UNet/BrushNet 多尺度结构的轻量级、自适应特征增强。该模块能够从“通道重要性”和“空间显著性”两个维度协同提升条件特征质量，并在保留原始特征表达稳定性的同时增强关键区域响应，为后续图像修复与细节重建提供更加判别且稳定的特征表征。

## DeGu

为进一步提升扩散修复过程中生成内容的语义一致性、局部编辑准确性以及视觉协调性，本文在 BrushNet 框架中引入 FreeInpaint 提出的 DeGu 机制。该模块的核心思想是在扩散采样过程中构建可微的多目标奖励函数，并利用奖励函数关于潜变量的梯度对去噪方向进行显式校正，从而使生成结果在满足文本语义约束的同时，更加符合修复区域的内容需求与整体图像的感知一致性。

具体而言，在第 $t$ 个去噪时刻，模型首先在文本条件和 BrushNet 条件特征的联合约束下预测当前潜变量 $z_t$ 的噪声残差 $ϵ_θ(z_t,t,c)$。随后，根据扩散调度器的参数化形式，将当前潜变量对应的中间重建结果映射回图像空间，得到当前时刻的可微图像估计 $x_t$。在此基础上，DeGu 从全局语义、局部内容匹配以及区域协调性三个层面构建联合奖励，以指导后续采样过程朝向更优的生成轨迹演化。

首先，引入全局语义奖励 $R_{overall}$，用于衡量生成图像与整体文本描述之间的语义一致程度。该项从全局视角约束修复结果，使生成内容能够与输入提示保持一致。其次，引入局部提示奖励 $R_{prompt}$，用于度量修复区域与文本提示之间的局部语义匹配关系。与全局语义约束不同，该项更加关注掩膜区域内部的编辑结果，从而增强待修复区域的内容可控性与指令响应能力。最后，引入协调性奖励 $R_{harmonic}$，用于刻画修复区域与非修复区域之间在纹理、颜色分布及感知质量上的融合程度，以减弱边界突变和局部不自然现象，提升修复结果的整体视觉连续性。

综合上述三类约束，DeGu 的总奖励函数定义为

$R=λ_gλ_oR_{overall}+λ_pR_{prompt}+λ_hR_{harmonic},$

其中，$λ_o$、$λ_p$ 和 $λ_h$ 分别表示全局语义奖励、局部提示奖励与协调性奖励的权重系数，$λ_g$ 表示整体引导强度。通过调节这些参数，可以在语义一致性、局部编辑精度和视觉自然度之间实现灵活平衡。

在得到总奖励后，对其关于当前潜变量 $z_t$ 求梯度，即

$g_t=∇_{z_t}R.$

该梯度反映了当前采样状态相对于目标奖励的优化方向，因此可用于修正扩散模型原始预测的去噪方向。对于采用噪声预测参数化的扩散调度器，引导后的噪声估计可表示为

$ϵ_θ=ϵ_θ-\sqrt{α_t} g_t,$

其中，$α_t$ 为第 $t$ 步的累计信号保留系数。经梯度修正后的噪声预测 $ϵ_θ$ 将替代原始噪声残差参与后续采样更新，从而使潜变量演化方向更加符合多目标奖励的优化需求。换言之，DeGu 将外部评价模型所提供的高层语义偏好与局部视觉质量约束直接注入扩散去噪过程，实现了对生成轨迹的在线优化。

此外，考虑到在每个去噪步均执行梯度引导会显著增加推理开销，本文采用间隔式引导策略，仅在若干固定采样步上执行 DeGu 更新。该策略在保持推理效率可接受的同时，仍能够持续对采样过程施加有效约束。与此同时，为保证引导重点集中于待编辑区域，局部语义奖励与协调性奖励主要围绕掩膜区域展开计算，从而避免对非编辑区域已有结构和纹理造成不必要扰动，增强修复结果的局部准确性与全局稳定性。

综上，DeGu 模块本质上是一种面向图像修复任务的扩散过程引导机制。它通过联合全局语义一致性、局部区域匹配性以及视觉协调性三类奖励信号，对传统扩散采样中仅依赖条件输入进行隐式控制的方式进行了补充。结合 BrushNet 提供的结构先验与条件特征，DeGu 能够进一步提升掩膜区域内容生成的真实性、文本一致性以及与上下文的融合自然度。

## 损失函数

3.4 训练与推理策略

在训练阶段，本文沿用 BrushNet 的基本扩散学习目标，仅对网络结构进行增强，即利用噪声预测损失监督模型学习：

$L_{diff}=E_{z_0,ϵ,t}[∥ϵ-ϵ_θ(z_t,c,I_m,M)∥_2^2].$

其中，自适应残差注意力融合模块随主网络端到端联合训练，以学习不同尺度条件特征的最优重加权方式。DeGu 不参与参数学习，而是在推理阶段作为训练无关的引导策略嵌入采样过程。由此，本文方法兼具结构级建模能力与采样级优化能力：前者通过改进 BrushNet 的条件注入机制提升多尺度特征表达，后者通过分解式引导进一步修正生成方向，最终提高自由形状图像修复的语义准确性、边界协调性与视觉真实性。
